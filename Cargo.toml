[package]
name = "bitwarden-desktop-native"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.14", features = ["tokio", "svg"] }
iced_aw = { version = "0.13", features = ["drop_down"] }
muda = "0.17.1"

[profile.release]
lto = true
codegen-units = 1
strip = true
